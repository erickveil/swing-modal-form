This is a simple demo of modal forms, using Java swing components.

A quick and simple user input form passes data back to the parent form.

Also, a more complex form is created. In this example it is functionally the same as the simple form.
The difference is in the way that it is constructed.
Its construction allows the addition of a multitude of controls to be added for a more complex modal form.
